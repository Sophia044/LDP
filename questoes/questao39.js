// Questão 39 — Altura abaixo de 1,60 m
let altura = 1.55;
if (altura < 1.60) {
  console.log(`Altura de ${altura.toFixed(2)} m: abaixo de 1,60 m.`);
} else {
  console.log(`Altura de ${altura.toFixed(2)} m: igual ou acima de 1,60 m.`);
}