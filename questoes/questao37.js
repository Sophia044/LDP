// Questão 37 — Multa por velocidade
let velocidade = 95;
if (velocidade > 80) {
  console.log(`Velocidade de ${velocidade} km/h: MULTA aplicada! Limite é 80 km/h.`);
} else {
  console.log(`Velocidade de ${velocidade} km/h: dentro do limite. Sem multa.`);
}