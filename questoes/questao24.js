// Questão 24 — Em recuperação?
let nota = 6.0;
if (nota >= 5 && nota <= 6.9) {
  console.log(`Nota ${nota}: Aluno em RECUPERAÇÃO.`);
} else {
  console.log(`Nota ${nota}: Aluno não está em recuperação.`);
}