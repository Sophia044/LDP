// Questão 34 — Triângulo escaleno
let a = 3, b = 4, c = 5;
if (a !== b && a !== c && b !== c) {
  console.log(`Triângulo com lados ${a}, ${b} e ${c}: ESCALENO.`);
} else {
  console.log(`Triângulo com lados ${a}, ${b} e ${c}: não é escaleno.`);
}